{
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq openssh-client sshpass git curl && chmod +x deploy/scripts/*.sh 2>/dev/null || true",
  "start": "./deploy/scripts/verify-deploy-secrets.sh || true",
  "terminals": []
}
